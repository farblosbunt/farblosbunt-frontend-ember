= power-select-typeahead selected=selectedLabel placeholder="SUCHEN" searchPlaceholder="SUCHEN" loadingMessage="Suche…" noMatchesMessage="Keine Ergebnisse" search=(perform searchTagsTask) onchange=(action "selectTag") onfocus=(action "clearSelection") onblur=(action "resetSelection") as |tag|
  = tag.word
  = tag.weight

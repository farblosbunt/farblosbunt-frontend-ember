= power-select-typeahead selected=selected.label placeholder="SUCHEN" searchPlaceholder="SUCHEN" loadingMessage="Suche…" noMatchesMessage="Keine Ergebnisse" search=(perform searchTagsTask) onchange=(action "selectTag") as |tag|
  = tag.label

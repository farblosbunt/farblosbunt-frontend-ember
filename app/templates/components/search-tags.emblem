= power-select-typeahead placeholder="SUCHE" searchPlaceholder="SUCHEN" loadingMessage="Suche…" noMatchesMessage="Keine Ergebnisse" search=(perform searchTagsTask) onchange=(action "selectTag") as |tag|
  = tag.label
